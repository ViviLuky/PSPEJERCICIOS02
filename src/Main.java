public class Main {
    public static void main(String[] args) {
        //Completa una clase llamada Mayusculas que haga lo siguiente:
        // Cree un proceso hijo (CambioMayusculas).
        // El proceso padre y el proceso hijo se comunicarán de forma bidireccional utilizando
        //streams.
        // El proceso padre leerá líneas de su entrada estándar y las enviará a la entrada estándar
        //del hijo (utilizando el OutputStream del hijo).
        // El proceso hijo leerá el texto por su entrada estándar, lo transformará todo a letras
        //mayúsculas y lo imprimirá por su salida estándar. Para realizar el programa hijo
        //(CambioMayuscula) se utilizará java. Será necesario utilizar la función toUpperCase().
        // El padre imprimirá en pantalla lo que recibe del hijo a través del InputStream del
        //mismo.
        //Ejemplo de ejecución:
        //hola (enter)
        //HOLA
        //mundo (enter)
        //MUNDO
    }
}